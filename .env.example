# ideahub-app 环境变量模板
#
# 用法：cp .env.example .env.local  然后按下面说明填。
# ⛔ .env.local 已被 gitignore，永远不要提交，也不要把 key 贴进聊天或文档。

# ── 火山方舟（Seedream 生图 / Seedance 生视频 / 豆包对话）────────────────
#
# ★ 刻意不带 VITE_ 前缀。带前缀的变量会被 Vite 打进客户端包，
#   等于把密钥发给每一个装了 App 的人。这里由 vite.config.ts 在开发服务器侧
#   注入到 /api/ark 代理，浏览器永远接触不到它。
#   新增任何密钥都遵循这条：要走网络就加代理，不要加 VITE_ 前缀。
#
# ★ 不填不会报错：__AI_REAL__ 变成 false，AI 功能自动降级到 mock。
#   所以"AI 没反应"第一件事是查这里有没有值。
ARK_API_KEY=

# ── 后端地址 ────────────────────────────────────────────────────────────
#
# ★ 本地开发一般【留空或注释掉】：留空走本地 IndexedDB 的种子数据，
#   首页立刻有内容。填了远端地址却连不上时，首页会是空的。
# VITE_API_BASE=https://ideahubs.org/api

# ── 受保护资产解密口令 ──────────────────────────────────────────────────
#
# 只有在需要加载 public/models/protected/ 下的加密模型时才需要。
# 平时留空即可，缺失时相关模型不加载，不影响其它功能。
VITE_ASSET_KEY=

# ── 豆包语音合成 openspeech（铸卡师的真人声）──────────────────────────
#
# ★ 与 ARK_API_KEY 是**两套**东西：不同域名（openspeech.bytedance.com）、
#   不同控制台（console.volcengine.com/speech）、不同鉴权。方舟里没有 TTS，
#   实测拉 /models 回来 129 个模型一个语音合成都没有。
#
# ★ 同样刻意不带 VITE_ 前缀：由 vite.config.ts 的 /api/tts 中间件注入。
#
# ★ 这里这条**只对 `npm run dev` 有效**。打成 APK 后 vite 中间件根本不存在，
#   App 会去 VITE_API_BASE 那台服务器要 /api/tts —— 所以要让**真机**有声，
#   必须把同名的 TTS_API_KEY 配到 **ideahub-server 的 .env** 里
#   （服务端实现见 server/src/routes/tts.routes.js，契约见 docs/api-contract.md）。
#   密钥也只能待在服务端：进了前端包，APK 解一下就拿到了。
#
# ★ 不填不会报错：铸卡师退回浏览器内置合成器
#   （需要系统装了中文语音包；桌面 Windows 走「设置→时间和语言→语音→添加语音」，
#   注意「讲述人」里的晓晓/云希是拿不到的）。
#
# 从哪儿拿：console.volcengine.com/speech → 开通「豆包语音合成模型2.0」→
#   「API Key」页拿一个 key。
# ★ 要的是**新版控制台的 API Key**，不是旧版的 APPID + Access Token：
#   /api/tts 走 V3 SSE（X-Api-Key 鉴权），旧版那对凭据是 V1 的，这条路根本走不通。
TTS_API_KEY=
#
# ★ 控制台要开通的是 **2.0**（resource id seed-tts-2.0）。1.0 是**另一件商品、
#   另行计费**，没开通会回 45000030 requested resource not granted。
#   所以音色清单（src/studio/voices.ts）只收 2.0 音色（*_uranus_*）。
#
# 音色一般**不用在这里配**——在 App 的「我的 → 设置 → 铸卡师的声音」里点着试听选就行
# （候选见 src/studio/voices.ts）。这里只作兜底默认，填了会覆盖用户的选择。
# VITE_TTS_VOICE=zh_female_gaolengyujie_uranus_bigtts

# 应用内更新的版本清单地址（侧载渠道）。空 = 关掉整套更新功能。
# 实际值在 .env.production（公开信息，随包发布）；格式与发版流程见 docs/app-distribution.md
# VITE_UPDATE_MANIFEST=https://github.com/<owner>/<repo>/releases/latest/download/latest.json
