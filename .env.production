# 生产构建（vite build / npm run build:app / npm run apk）专用。
#
# ★ 为什么必须有这个文件：`.env.local` 里那条 VITE_API_BASE 是给 `npm run dev` 用的
#   localhost:4000。它对**所有 mode 都生效**，于是不加这个文件的话，打出来的 APK 里
#   API_BASE 就是 http://localhost:4000 —— 在手机上那指的是**手机自己**，什么都没有。
#   症状还特别难认：client.ts 探活失败会安静地整体回退 IndexedDB 离线模式，
#   照样能登录（本地账号）、能看种子作品，只有"云端的东西"一律没有；
#   NPC 语音那条更隐蔽，请求直接连接失败、被 catch 吞掉，表现成"没声音"。
#   （2026-08-10 实测：打出来的 dist 里 grep 到的就是 localhost:4000。）
#
# ★ Vite 的加载顺序是 .env → .env.local → .env.[mode] → .env.[mode].local，
#   后者覆盖前者，所以这个文件能盖住 .env.local 那条 —— 顺序反了就白写。
#
# 这里只放**公开**信息（一个公网 URL），所以入仓；密钥仍然只准待在 .env.local 与服务端。
VITE_API_BASE=https://api.ideahubs.org

# ── 应用内更新（侧载渠道）─────────────────────────────────────
#
# 版本清单地址。空 / 不配 = 整套更新功能关掉（浏览器开发、以及还没搭好分发时）。
#
# ★ 这个 URL 是 GitHub Release 的"永远指向最新一版"固定地址：
#   /releases/latest/download/<资产名> 会自动 302 到最新 Release 里的同名文件，
#   所以发新版只要传一个同名的 latest.json 上去，不用改代码、不用改这里。
# ★ 清单由**原生层**去拉（见 AppUpdaterPlugin.check）。WebView 的 origin 是
#   https://localhost，在 Web 层 fetch 它是跨域，而 GitHub 不发 CORS 头。
# ★ 如果你的用户在国内、GitHub 太慢，把 latest.json 与 APK 换到自己的服务器上，
#   改这一行即可（格式见 docs/app-distribution.md）。
VITE_UPDATE_MANIFEST=https://github.com/TianbinLiu/ideahub-app/releases/latest/download/latest.json
