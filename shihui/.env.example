# 与 ideahub-app 同一套约定：不配 = 全走 mock（页面会有「演示模式」角标）。
# 骨架阶段 real 实现尚未接线，配了也只是把角标去掉，接线见 src/ai/real.ts 的 TODO。

# 火山方舟（生图 Seedream / 生视频 Seedance / 打分 豆包 chat）
ARK_API_KEY=

# 火山 openspeech（范读 TTS + 朗诵识别 ASR）。注意：方舟没有语音产品线，这是另一条线的凭证
TTS_APPID=
TTS_TOKEN=

# 服务端地址（骨架纯前端，本地库存 localStorage；接后端后与 ideahub 同款 API_BASE 收口）
VITE_API_BASE=
