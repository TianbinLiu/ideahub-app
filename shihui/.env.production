# 出包用的生产常量（无密钥，入仓）。★ 与主 App 同一条铁律：这里的值随包发布、
# 发出去就钉死在每个用户手里 —— 所以只放公开信息，且清单走服务端转发（可换源）。
#
# 应用内自更新的版本清单地址（诗绘自己的通道，见 server 的 appRelease.routes.js）。
# 空 = 整个自更新功能关闭。
VITE_UPDATE_MANIFEST=https://api.ideahubs.org/api/app/shihui/latest.json
