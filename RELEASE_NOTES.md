- 白模模板换了新做法：人偶**不再上色、也不再印编号**，全都是一模一样的白色，挂卡时按「**从左往右第几个**」来指认。实测这样挂 12 组角色零错误（上一版按颜色约有一半会挂错人）
- **已有的模板照旧能用，不用重做**：2026-08-17 之前做的编号模板一个字都没改，套用、挂卡、出片全部和以前一样
- 上一版提到的「颜色标记」在真机上准确率只有一半左右，**没有任何模板真的用它做出来过**，这一版直接换掉，你不需要做任何事
- 挂卡时如果模板带了画面位置数据，可以**直接把角色卡拖到画面上那个人偶身上**，松手后会问你一句「是这个人吗」—— 换错人是不会报错的，这一问是唯一的拦截。老模板仍然只能在下面的列表里挂卡（它们没有画面位置数据）
- 自己做的新模板发布前请**逐个核对一遍位置**：AI 排的「第几个」只是猜测。特别注意 —— 删掉一个位子之后，如果画面上那个人偶根本不存在，它右边所有位子都要往左挪一位
- 修了几个「不会报错、只会悄悄换错人/多报价」的问题：① 挂卡后那段点名要求生成失败时，输入框里会留着**上一次**的旧映射，而且照样能点「生成本段」——现在会清空并让你重写或一键填默认写法；② 编号老模板在角色位挂满时，「去掉人偶身上编号」那句话可能被提示词长度限制切掉，导致成片里人物顶着编号；③ 白模化「看帧」那一笔报价比实际多算了十几倍（**多看几帧本来就不额外收费**），现在照实报；④ 你在核对面板删掉一个角色位之后，画面上的拖拽落点框会自动关掉（那时框指的还是删之前的位置，照着拖会换错人），挂卡回到点列表那条路
- **自己做好的白模片现在能直接用了**：以前那颗「上传白模预演视频」按钮其实走的是「让 AI 帮你换成白模」那条路——你传一段本来就是白模的片子，它会**再白模化一次**（钱扣了、画质还更差）。现在框选完会问你一句「这段视频要怎么用」：让 AI 换人偶（一次真实出片），还是它本来就是白模、直接用（不出片，只认人，便宜两个量级）
- **上传时不再因为"分辨率差一点点"就被拒**：以前宽×高不到 40.8 万像素就传不上来，而很多手机拍的/下载的片子正好差一点点。现在只看边长（≥300），裁完之后系统会自动放大到刚好过线。太小的素材（放大只会变马赛克）仍然会当场拒
- **超过 30 秒的视频也能做模板了**：框选那一段就行（以前这条路没有裁剪步骤，长视频根本传不进来）
- **认角色位失败可以重来**：以前认人和建模板挤在一次请求里，网络一抖就得到一个"没有角色位"的模板，还看不出为什么。现在模板先建好，认角色位单独一步——失败了在「我的模板」里点一下「识别角色位」重试即可，模板一直好好的
- 「我的模板」和市场列表里，自己那条模板下面直接能**下架**和**删除**（以前只有点进详情页才有）。删除会连云端视频一起销毁，所以改成点两下确认
