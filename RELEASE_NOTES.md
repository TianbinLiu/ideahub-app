- **工作流多了一个「画布」模式**：一屏看完整条流水线，点哪一段就在原地展开编辑窗，横竖两个方向都能滑。右上角可以随时在「画布 / 线性」之间切，选了哪个会记住（不是一次会话）。第一次进去有 6 步引导
- **可以对着画布说话**：底部那条输入框里写「第 2 段套宗主模板」「给第 3 段加一段，拍他推门进去」，AI 会把它变成操作。**要花钱的三件事（推演、出片、挂卡合成）一律先摆一张确认卡、你点了才跑**，卡上写着这一下要多少 token。回执里那排 ✓/✗ 说的是流水线的真实状态，不是 AI 嘴上说的
- **出片时可以离开了**：以前炼一段视频得守着这一屏。现在会有一个全局的出片胶囊跟着你，去首页刷视频、去工坊摆卡都行，炼完回来接着弄
- **成片可以就地回看，还能圈着改**：点段卡上的 ▶ 直接看这一段，看到哪一帧不对就点「⭕ 圈选改画面」，在画面上圈出来写一句要求，重炼时 AI 会照着改
- **长视频能做成模板了**：超过 30 秒的素材，把选段拉满整条就会自动切成多段登记成**一组**（每段一个独立模板、逐段认人、按段计价）。套用时整组一起铺进工作流，逐段挂卡出片，合并时自动回填原片的音轨
- **大视频终于传得上去了**：以前 40MB 以上的素材经常传到一半就说「网络不可用」，而且**每次都是在同一个时间点断**——查下来是中间网关有一道 125 秒的硬限制，跟你的网速和文件大小都没有直接关系。现在视频改成分块直传，字节不再经过那道关卡；上传时能看到真实的百分比进度，中途断了只重传那一小块。实测同一条 47MB 的素材：以前三次全挂，现在约 100 秒传完
- 修了一批**不会报错、但会让你白花钱或丢东西**的问题：① 换模板/重铺流水线时，已经花钱炼出来的段会被覆盖掉，而确认卡还说「没有花掉的钱」——现在它按真实存盘情况如实说，正在生成时干脆不让换；② 圈选改画面那一笔的报价比实际少算了（界面报 108k、实扣 175k），现在两边同一个函数；③ 确认卡摆着的时候你去改了时长，回来点执行仍按老价钱扣——现在执行前用同一把尺重算；④ 删掉前面某一段之后，后续操作会静默落到**另一段**上（最狠的是把已出片的那段覆盖掉并照常扣费）；⑤ 发布作品时如果服务器回了一个形状不对的成功响应，作品会在重启后消失，而界面写着「全部上传成功」
- 「我的模板」里，分段做出来的模板会**折成一张卡**（点「展开各段」看每一段），不再一段占一行
