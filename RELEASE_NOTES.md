- 白模模板的角色位编号可以随时回去核对了：入口常驻，不再是"点过一次就找不到了"
- 画面上找不到的号、或者两个人偶重号，把那个位子删掉就行，剩下的编号一个都不会变，不用重炼整段
- 修：套用模板出的成片里，人物头上会带着白模的编号
- 修：用 4 秒素材做出来的模板，别人套用时一定失败（AI 会把成片截短零点几秒，做出来只有 3.7 秒，低于出片引擎的下限）。现在素材至少要 5 秒，做坏了也会当场说清楚而不是让你发出去
- 修：模板视频的时长显示与计费用的数对不上
