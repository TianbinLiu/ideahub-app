- 白模模板换了新做法：人偶**不再上色、也不再印编号**，全都是一模一样的白色，挂卡时按「**从左往右第几个**」来指认。实测这样挂 12 组角色零错误（上一版按颜色约有一半会挂错人）
- **已有的模板照旧能用，不用重做**：2026-08-17 之前做的编号模板一个字都没改，套用、挂卡、出片全部和以前一样
- 上一版提到的「颜色标记」在真机上准确率只有一半左右，**没有任何模板真的用它做出来过**，这一版直接换掉，你不需要做任何事
- 挂卡时如果模板带了画面位置数据，可以**直接把角色卡拖到画面上那个人偶身上**，松手后会问你一句「是这个人吗」—— 换错人是不会报错的，这一问是唯一的拦截。老模板仍然只能在下面的列表里挂卡（它们没有画面位置数据）
- 自己做的新模板发布前请**逐个核对一遍位置**：AI 排的「第几个」只是猜测。特别注意 —— 删掉一个位子之后，如果画面上那个人偶根本不存在，它右边所有位子都要往左挪一位
