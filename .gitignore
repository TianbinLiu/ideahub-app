node_modules
dist
*.local
.DS_Store
# 受保护资产：加密产物与（未来）购买的模型本体都不进公开仓库
public/models/protected/
assets-private/
# 签名 keystore 与口令：公开仓库绝不入仓（备份见 android/keystore/README.md）
# ★ 必须用 **/ 前缀：`android/keystore/` 这样带斜杠的规则**只锚定仓库根**，
#   诗绘的 shihui/android/keystore/ 不会被它挡住 —— 2026-08 给 shihui 复制 keystore
#   时实测过，密钥和口令当场处于"可提交"状态。任何新增子工程都靠这条兜住。
**/android/keystore/

# ── 密钥 ────────────────────────────────────────────────────────────────
# .env.local 本来就被上面的 *.local 兜住了，但那是【碰巧】——
# 有人建一个 .env 就会被直接提交上去。这里显式挡住整个 .env 家族，
# 只放行模板 .env.example（它不含真实值）。
.env
.env.*
!.env.example
# ★ .env.production 是例外，必须入仓：里面只有一个公网 API 地址（没有任何密钥），
#   而它决定了 APK 连哪台服务器。不入仓的话，换台机器/新 worktree 打出来的包会
#   悄悄回落到 .env.local 的 localhost:4000 —— 在手机上指向手机自己，全线断连
#   却还能靠离线模式跑起来，极难发现。密钥仍然只准待在 .env.local 与服务端。
!.env.production

# ── Claude Code ─────────────────────────────────────────────────────────
# .claude/skills/ 与 .claude/launch.json 是【团队共享】配置，必须入库 ——
# 新成员克隆后就带着工程规则 skill 和调试配置，不用手工再配一遍。
# 只挡两样：本机工作区，和可能含个人 token 的本地设置覆盖。
.claude/worktrees/
.claude/settings.local.json

# 音色试听样带：听完就删，不入仓
design/tts-samples/

# shihui 的 forge 预生成产物（mp4/关键帧/manifest）：内容包走 CDN 分发，不进 git
shihui/public/clips/

# 压缩后的内容库（APK 打包用，母版在 public/clips）
.clips-packed/
# 生成的图标/封面原图（切好的图标进仓，原图不进）
assets-art/
